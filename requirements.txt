# telethon < 1.42 يدمج اسم الملف القادم من القناة المصدر عند التنزيل إلى مجلد،
# فيسمح بكتابة ملفات خارجه (path traversal). لا تخفّض هذا الحد.
telethon>=1.42,<2
requests>=2.31
# xauth.py يتكامل مع تدفق onboarding الداخلي؛ لا تحدّثه دون تشغيل test_xauth.py.
twikit==2.3.3
# Twikit 2.3.3 no longer discovers X's ondemand transaction asset itself.
xclienttransaction==1.0.3
# Last release supporting the project's Python 3.9 CI floor; Chromium is
# installed separately on Raspberry Pi and is only launched for explicit login.
playwright==1.60.0
