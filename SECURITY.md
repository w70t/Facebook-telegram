# الأمان

> تكامل X متوقف بالكامل حالياً. تبقى ملاحظات X أدناه توثيقاً دفاعياً للكود
> القديم المعزول ولأي بقايا من الإصدارات السابقة.

## نموذج الثقة

افترض أن **كل ما يدخل من قناة مصدر أو من X عدائي**. أنت تتابع قنوات لا تملكها،
وصاحب القناة يتحكّم بنص المنشور، ونوع الوسيط، **واسم الملف**.

من ناحية أخرى، هذه الأطراف موثوقة بالكامل ولا يحميك البوت منها:

| الطرف | ما يستطيعه |
|------|-----------|
| **المالك** | كل شيء: الأدمنون، فيسبوك، القنوات، التحديث |
| **الأدمن** | تغيير توكن فيسبوك، تحويل قروب المراجعة، النشر، **زر التحديث** |
| **من يملك دفعاً إلى مستودع GitHub** | زر 🔄 يسحب وينفّذ الكود ⇒ **تنفيذ أوامر على الـ Pi** |
| **من يقرأ ملفات المستخدم `pi`** | `settings.json`, `*.session`, `x_cookies_*.json` |

> ⚠️ لا تجعل مستودعك عامّاً للكتابة، ولا تضف أدمناً لا تثق به ثقة كاملة.

## ثغرات عولجت

### 1. كتابة ملفات خارج مجلد التنزيل (خطيرة)
`telethon` قبل **1.42.0** كان يدمج اسم الملف القادم من المرسِل كما هو عند
التنزيل إلى مجلد. قناة مصدر عدائية تُرسل ملفاً باسم:

```
../../venv/lib/python3.11/site-packages/x.pth
```

فيُكتب داخل الـ venv — وملفات `.pth` **تُنفَّذ عند بدء بايثون**، أي تنفيذ كود
عند أول إعادة تشغيل (والبوت يعيد تشغيل نفسه: `Restart=always` + زر التحديث).

**العلاج:** لم نعد نمرّر مجلداً إطلاقاً — نولّد اسم الملف بأنفسنا
(`tg_<عشوائي><امتداد مُنقّى>`) فلا يُستخدم اسم المرسِل بأي إصدار، مع فحص احتواء
ثانٍ بعد التنزيل، ورفع الحد الأدنى إلى `telethon>=1.42`.

### 2. ملفات الجلسة مقروءة للجميع (خطيرة)
`telethon` ينشئ `*.session` عبر sqlite بصلاحيات **0644** ولا يضبطها أبداً
(لا `chmod` ولا `umask` في المكتبة كلها). ملف الجلسة يعادل **دخولاً كاملاً
لحساب تلغرام**: أي مستخدم آخر على الجهاز ينسخه وينتحل الحساب.

**العلاج:** تقييد إلى `0600` عند الإقلاع وكل ساعة.

### 3. صلاحية باقية بعد إزالة الأدمن (متوسطة)
الصلاحية كانت تُفحص عند **فتح** محادثة الإعداد فقط. من أُزيل من الأدمنين وعنده
خطوة معلّقة (مثل «أرسل توكن فيسبوك») كان بإمكانه إكمالها.

**العلاج:** إعادة فحص `is_admin` عند كل رسالة، وإلغاء الحالة فوراً.

### 4. تسريب بيانات اعتماد في مخرجات git (متوسطة)
زر التحديث يرسل مخرجات `git pull` إلى المحادثة. لو كان رابط `origin` يحمل توكناً
(`https://user:ghp_xxx@github.com/...`) فإن رسالة الخطأ تكشفه.

**العلاج:** حجب `user:pass@` قبل الإرسال.

### 5. SSRF عبر روابط وسائط X (متوسطة)
الروابط تأتي من ردود `twikit` لا من عندنا. ردّ مُلاعَب أو تحويلة كان يجعل البوت
يجلب عناوين داخل شبكة الـ Pi (راوتر، خدمات محلية).

**العلاج:** `https` فقط + قائمة نطاقات موثوقة (`*.twimg.com`, `x.com`) تُفحص
**عند كل تحويلة**، بحد أقصى ثلاث تحويلات.

### 6. إغراق القرص (متوسطة)
طابور المراجعة كان بلا سقف: قناة مُغرِقة تملأ بطاقة الـ SD.

**العلاج:** سقف 500 منشور (يُزاح الأقدم مع ملفاته) + مهلة 48 ساعة + كنس الملفات
اليتيمة، مع رفض التنظيف لو وُجّه إلى مجلد نظام.

### 7. أخرى
- `fb_page_id` يُدمج في مسار Graph API ⇒ صار يُقبل أرقاماً فقط.
- كوكيز X تُكتب إلى ملف مؤقت 0600 ثم تُستبدل ذرياً؛ البقايا بعد crash
  مستثناة من Git وتُنظّف عند البدء.
- رموز X كانت تُقرأ من `input()` فتعلّق البوت أو تفشل تحت systemd ⇒ صار تسجيل
  الدخول تفاعلياً عبر محادثة Telegram الخاصة نفسها بمهلة، مع حذف رسالة
  Authenticator وعدم تخزين الرمز، وإلغاء المحاولة إن تعذّر الحذف.
- تسجيل X التفاعلي يستخدم Chromium بسياق مؤقت فقط: بلا profile دائم أو
  screenshots أو traces أو downloads. تُعاد cookies في الذاكرة ثم يتحقق منها
  القارئ قبل الحفظ الذري. كلمة المرور ورمز Authenticator لا يُحفظان بعد النجاح.
- poller يستخدم cookies فقط؛ لا يحاول كلمة المرور ولا ينتظر إدخالاً تفاعلياً.
- `/claim` كان رمزاً من 6 أرقام بلا حد للمحاولات ⇒ `token_urlsafe` + مقارنة
  ثابتة الزمن + 5 محاولات.
- تقييد وحدة systemd: `NoNewPrivileges`, `ProtectSystem`, `ProtectHome`.

## مخاطر مقبولة عن قصد

| المخاطرة | لماذا لم تُعالَج |
|---------|------------------|
| **رمز `/claim` يظهر في journald** | هذا هو المقصود — من يقرأ سجل الـ Pi هو مالكه. `journalctl` مقيّد لمجموعة `adm`/`systemd-journal`. |
| **زر التحديث = تنفيذ كود** | ميزة مقصودة. أمانها = أمان حسابك على GitHub. فعّل 2FA. |
| **توكن فيسبوك في query string** | ممارسة Graph API القياسية فوق HTTPS. |
| **البوت يردّ على غير الأدمنين** | يسهّل الإعداد؛ الثمن رسالة رفض واحدة لكل غريب. |

## الإبلاغ

لو وجدت ثغرة، افتح issue خاصاً أو راسل مالك المستودع مباشرة — لا تنشر التفاصيل
قبل الإصلاح.

## للمطوّرين

`tests/test_security.py` + `tests/_smoke_security.py` يعيدان إنتاج كل ثغرة أعلاه
ببيانات هجومية حقيقية. شغّلهما قبل أي دفع:

```bash
python -m pytest tests/test_security.py -v
```
