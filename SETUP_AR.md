# 📘 دليل الإعداد — كل شيء من داخل تلغرام

لا حاجة لتعديل أي ملفات. فقط أمر واحد لمرة واحدة، ثم كل شيء من البوت بالأزرار.

---

## 1) التثبيت على Raspberry Pi

استخدم **Python 3.10 أو أحدث**.
```bash
git clone https://github.com/w70t/Facebook-telegram.git
cd Facebook-telegram
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt
```

## 2) الإعداد الأولي (مرة واحدة فقط)
هذه القيم الثلاث لازمة لتشغيل البوت نفسه:
```bash
python configure.py
```
سيسألك:
- **API_ID** و **API_HASH** ← من <https://my.telegram.org> ← API development tools
- **BOT_TOKEN** ← من [@BotFather](https://t.me/BotFather)

تُحفظ في `settings.json`. ما راح تحتاج تكررها.

## 3) شغّل البوت
```bash
python main.py
```
في السجل بيظهر سطر مثل:
```
لا يوجد مالك بعد. اضغط في البوت «🔑 تفعيل الملكية» ثم أرسل الرمز: AbCd-123_xyz
```

## 4) صيّر نفسك المالك
افتح بوتك في تلغرام واضغط **🔑 تفعيل الملكية**، ثم أرسل الرمز الظاهر في السجل.
✅ تصير المالك والأدمن، وتظهر أزرار **⚙️ لوحة التحكم** و**🆔 معرّفي**
و**🛑 إلغاء** تحت خانة الكتابة. لا تحتاج إلى كتابة الأوامر.

---

## 5) أكمل الإعداد من زر **⚙️ لوحة التحكم** (بالترتيب)

### 🔐 تسجيل دخول الحساب الشخصي
1. اضغط الزر → أرسل رقم هاتفك:
   - مع رمز الدولة: `+9665xxxxxxxx`
   - أو بدونه، بشرط تضبط **🌍 رمز الدولة الافتراضي** أولاً (مثل `966`).
2. يوصلك رمز داخل تلغرام. **أرسله مع فواصل** حتى لا يُلغى تلقائياً:
   ```
   1 2 3 4 5
   ```
3. لو حسابك محمي بتحقق بخطوتين، بيطلب كلمة المرور.

✅ بعدها الحساب مسجّل وملف الجلسة محفوظ.
> استخدم **رقماً ثانوياً** ولازم يكون **عضواً في القنوات** التي ستضيفها.

### 📘 إعداد فيسبوك
اضغط الزر ثم أرسل:
1. **معرّف الصفحة** (FB_PAGE_ID)
2. **توكن الصفحة** (FB_PAGE_TOKEN)

بعد الحفظ يتحقق البوت من التوكن فوراً ويخبرك باسم الصفحة، أو يشرح المشكلة.

> ⏱️ **مهم جداً:** التوكن الذي يعطيك إياه Graph Explorer مباشرةً **قصير الأجل
> (~ساعة واحدة)**. لو استخدمته كما هو سيتوقف النشر بعد ساعة بلا سبب واضح.
> اتبع الخطوات التالية للحصول على توكن صفحة **لا ينتهي**.

#### الحصول على توكن صفحة طويل الأجل

**١) جهّز تطبيقاً** على <https://developers.facebook.com/apps> واحفظ منه
`App ID` و `App Secret` (من Settings → Basic).

**٢) توكن مستخدم قصير الأجل** من <https://developers.facebook.com/tools/explorer>:
فعّل الصلاحيات `pages_manage_posts` و `pages_read_engagement` و `pages_show_list`،
ثم انسخ التوكن الظاهر أعلى الصفحة.

**٣) بدّله بتوكن مستخدم طويل الأجل (60 يوماً)** — نفّذ في الطرفية:
```bash
curl -G "https://graph.facebook.com/v23.0/oauth/access_token" \
  -d "grant_type=fb_exchange_token" \
  -d "client_id=APP_ID" \
  -d "client_secret=APP_SECRET" \
  -d "fb_exchange_token=التوكن_القصير"
```

**٤) استخرج توكن الصفحة** باستخدام التوكن الطويل من الخطوة السابقة:
```bash
curl -G "https://graph.facebook.com/v23.0/me/accounts" \
  -d "access_token=التوكن_الطويل"
```
لكل صفحة `id` (= FB_PAGE_ID) و `access_token` (= FB_PAGE_TOKEN).
**توكن الصفحة المستخرج من توكن مستخدم طويل الأجل لا ينتهي** ما دمت لم تغيّر
كلمة مرورك ولم تسحب صلاحيات التطبيق.

**٥) تحقّق** أن التوكن دائم فعلاً:
```bash
curl -G "https://graph.facebook.com/v23.0/debug_token" \
  -d "input_token=توكن_الصفحة" -d "access_token=التوكن_الطويل"
```
ابحث عن `"expires_at": 0` — الصفر يعني بلا انتهاء.

> لو انتهى التوكن يوماً، سينبّهك البوت برسالة 🔑 في قروب المراجعة بدل أن يفشل بصمت،
> والمنشور يبقى محفوظاً فتضغط **نشر** مجدداً بعد تحديث التوكن.

### 📍 تعيين قروب المراجعة
- أنشئ قروب، أضف الأدمنين والبوت، وارفع البوت أدمن.
- من داخل القروب افتح لوحة التحكم بالطريقة القديمة `/panel` ثم اضغط 📍؛ لوحة
  الأوامر الثابتة لا تُرسل إلى القروبات حفاظاً على نظافتها.

### 📡 القنوات المصدر
- اضغط **➕ إضافة قناة** ثم أرسل `@يوزر_القناة` أو رابطها أو معرّفها الرقمي.
- تقدر تضيف **أكثر من قناة**. **➖ حذف** لإزالتها.
> شرط: حسابك الشخصي **عضو في القناة**.

### ⛔ X (تويتر)

تكامل X متوقف بالكامل. لا توجد أزرار دخول أو قارئ خلفي أو اتصالات إلى X،
وأي زر X قديم في سجل المحادثة يُرفض ولا يبدأ محاولة.

### 🚫 فلترة الكلمات — اختياري
- اضغط **🚫 فلترة الكلمات** → **➕ إضافة كلمة** وأرسل الكلمة الممنوعة (مثل `اعلان`).
- أي منشور تلغرام يحتوي أي كلمة ممنوعة **يُتجاهل تلقائياً** ولا يصل للمراجعة.
- المطابقة غير حسّاسة لحالة الأحرف وتعمل بالعربي والإنجليزي.

### 👤 الأدمنون (للمالك)
أضف أدمنين آخرين بمعرّفهم الرقمي (يجدونه من زر **🆔 معرّفي**).

---

## 6) الاستخدام
أي منشور جديد في القناة → يوصل لقروب المراجعة مع الأزرار:

| الزر | الوظيفة |
|------|---------|
| ✅ نشر | ينشر النص مع الوسائط |
| 📄 نشر النص فقط | بدون الفيديو/الصورة |
| ✏️ تعديل النص | ترسل نصاً جديداً فيُحدَّث في نفس رسالة المراجعة |
| ❌ تجاهل | يلغي المنشور ويحذف ملفاته |

ملاحظات:
- **الألبومات:** عدة صور برسالة واحدة تصل كألبوم + رسالة تحكّم بالأزرار تحتها،
  وتُنشر على فيسبوك **كمنشور واحد**.
- **النص الطويل** يُختصر في المعاينة فقط (بـ `…`)؛ يُنشر كاملاً على فيسبوك.
- **إعادة التشغيل لا تُبطل الأزرار** — المنشورات المعلّقة محفوظة في `pending.json`.
- المنشور الذي لم تتخذ فيه قراراً خلال **48 ساعة** يُحذف تلقائياً مع ملفاته
  (غيّرها بمفتاح `pending_ttl_hours`).
- **فشل النشر لا يُضيّع المنشور** — يبقى معلّقاً فتضغط نشر مجدداً.

---

## 7) التشغيل الدائم + التحديث الذاتي
```bash
sudo cp deploy/telegram-fb-bot.service /etc/systemd/system/
sudo nano /etc/systemd/system/telegram-fb-bot.service   # عدّل User والمسارات
sudo systemctl daemon-reload
sudo systemctl enable --now telegram-fb-bot
journalctl -u telegram-fb-bot -f
```
لتحديث الكود لاحقاً: اضغط زر **🔄 تحديث من GitHub** داخل البوت — يسحب آخر نسخة،
يثبّت أي اعتماديات جديدة، ثم يعيد تشغيل نفسه. **لو فشل السحب لن يعيد التشغيل**
وسيعرض لك سبب الفشل.

---

## حل المشاكل
| المشكلة | الحل |
|--------|------|
| زر تفعيل الملكية لا يعمل | خذ الرمز من سجل التشغيل (`journalctl -u telegram-fb-bot`) وتأكد أنك في المحادثة الخاصة |
| «تجاوزت عدد المحاولات» | 5 محاولات كحد أقصى — أعد تشغيل الخدمة لتوليد رمز جديد |
| الرمز يُلغى عند تسجيل الدخول | أرسله مع فواصل: `1 2 3 4 5` |
| رقم بدون رمز دولة | اضبط 🌍 رمز الدولة الافتراضي أو أرسل الرقم كـ `+...` |
| لا يصل منشور للمراجعة | تأكد أن الحساب عضو في القناة وأنك عيّنت 📍 قروب المراجعة |
| النشر توقف فجأة بعد ساعة | التوكن قصير الأجل — اتبع خطوات **التوكن طويل الأجل** أعلاه |
| رسالة 🔑 في قروب المراجعة | توكن فيسبوك انتهى/نقصت صلاحياته — أعد ضبط 📘 فيسبوك |
| فشل النشر على فيسبوك | المنشور يبقى معلّقاً؛ أصلح السبب واضغط ✅ نشر مجدداً |
| وسائط لا تصل | حجمها يتجاوز `max_media_mb` (200 ميغا افتراضياً) |
| «ملف الإعدادات تالف» في السجل | يُسترجع تلقائياً من `.bak`؛ التالف محفوظ بلاحقة `.corrupt-*` |

> ❗ لا ترفع إلى GitHub: `settings.json` و`pending.json` و`*.session`
> وأي `x_cookies_*.json` قديم من إصدار سابق (كلها مستثناة في `.gitignore`).
